{{-- @if (Route::has('login'))
@auth
<div class="host-name">
    VanLifeBases
</div>

<div class="reserve-btn">
    <button>{{ ¥??? }}</button>
</div>
@else
<a href="{{ route('') }}">予約する</a>
@endauth

@endif --}}


